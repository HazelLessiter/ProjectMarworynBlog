namespace PageEngine.Parsing;

public static class FrontMatterParser
{
    // Returns (frontMatter, bodyMarkdown). Throws if no valid front matter block found.
    public static (Dictionary<string, string> FrontMatter, string Body) Parse(string rawContent)
    {
        throw new NotImplementedException();
    }
}